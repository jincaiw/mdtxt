---
import Base from "../layouts/Base.astro";
---

<Base title="mdtxt：本地优先的 Markdown 编辑器" description="mdtxt 是一个默认简体中文、完整支持英语的跨平台 Markdown 编辑器。">
  <main class="mx-auto max-w-4xl px-6 py-24 text-fg">
    <p class="mono-label"><span class="tok-mark">$</span> mdtxt / 0.1.0</p>
    <h1 class="font-serif-display mt-5 text-[clamp(3.4rem,12vw,7rem)] leading-none">mdtxt</h1>
    <p class="mt-8 max-w-2xl text-xl leading-8 text-fg-muted">本地优先、默认简体中文的 Markdown 编辑器。直接打开文件，在源码、Live Beta、阅读与分栏视图间切换；Markdown 原文始终可见、可回退。</p>

    <section class="mt-14 grid gap-5 md:grid-cols-3">
      <article class="rounded-2xl border border-line p-6"><h2 class="text-lg font-semibold">文件优先</h2><p class="mt-3 text-sm leading-6 text-fg-muted">Markdown 原文始终是唯一事实来源，不建立专有文件格式。</p></article>
      <article class="rounded-2xl border border-line p-6"><h2 class="text-lg font-semibold">双语完整</h2><p class="mt-3 text-sm leading-6 text-fg-muted">简体中文为默认界面，同时完整支持 English。</p></article>
      <article class="rounded-2xl border border-line p-6"><h2 class="text-lg font-semibold">跨平台</h2><p class="mt-3 text-sm leading-6 text-fg-muted">基于 Tauri、Rust 和 React，面向 Windows、macOS 与 Linux。</p></article>
    </section>

    <section class="mt-14 rounded-2xl border border-line bg-fg/[0.03] p-7">
      <h2 class="text-2xl font-semibold">发布状态</h2>
      <p class="mt-3 max-w-2xl leading-7 text-fg-muted">mdtxt 0.1.0 将在 mdtxt 专属仓库提供 macOS Apple Silicon、Windows x64 与 Ubuntu LTS x64 预发布包。当前产物未签名/公证，自动更新保持关闭。</p>
      <div class="mt-5 flex flex-wrap gap-3">
        <a href="./guide/" class="btn btn-primary">阅读使用指南</a>
        <a href="https://github.com/jincaiw/mdtxt/releases" rel="noopener" class="btn btn-ghost">查看预发布包</a>
      </div>
    </section>
  </main>
</Base>
